import GithubParameters from '../../src/types/github-parameters';

const mockGithubParameters: GithubParameters = {
  GIT_COMMIT: 'adsdasads',
  GIT_REPO: 'price-alert',
  PR_ID: '23',
  GITHUB_TOKEN: 'GITHUB_TOKEN',
};

export default mockGithubParameters;
